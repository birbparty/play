# Example Assets

Examples first look for audio files in this directory. If a file is not present,
the shared example helpers fall back to the committed fixtures in
`tests/fixtures/generated/` so examples can compile and run from a fresh
checkout without duplicating binary audio files.

Expected filenames:

- `tone_sfx.wav`
- `tone_music.ogg`
- `tone_music_long.ogg`
