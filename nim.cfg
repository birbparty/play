# Desktop/default Nim config.
#
# Console builds do not auto-read nim_3ds.cfg or nim_vita.cfg. Build scripts
# should pass those files explicitly or copy them over nim.cfg before invoking
# `nim c -d:ds3` / `nim c -d:vita`, matching the clckr convention.

--path:"src"
--mm:orc
